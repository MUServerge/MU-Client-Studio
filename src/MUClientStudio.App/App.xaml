<Application x:Class="MUClientStudio.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="Bg" Color="#06080C"/>
        <SolidColorBrush x:Key="Panel" Color="#0B1118"/>
        <SolidColorBrush x:Key="Panel2" Color="#101822"/>
        <SolidColorBrush x:Key="Line" Color="#273548"/>
        <SolidColorBrush x:Key="Text" Color="#F5F7FB"/>
        <SolidColorBrush x:Key="Muted" Color="#8D9AAD"/>
        <SolidColorBrush x:Key="Blue" Color="#76C8FF"/>
        <SolidColorBrush x:Key="Green" Color="#70D49B"/>
        <SolidColorBrush x:Key="Gold" Color="#D2AF69"/>

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
        </Style>

        <Style TargetType="ToolTip">
            <Setter Property="Foreground" Value="#C8D4E2"/>
            <Setter Property="Background" Value="#0C131C"/>
            <Setter Property="BorderBrush" Value="#2B3C51"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="9"/>
        </Style>

        <Style x:Key="V6DropDownThumb" TargetType="Thumb">
            <Setter Property="MinHeight" Value="26"/>
            <Setter Property="Background" Value="#344960"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="ThumbBorder"
                                Margin="1,0"
                                Background="{TemplateBinding Background}"
                                CornerRadius="3"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Background" Value="#48647F"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Background" Value="#587795"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="V6DropDownScrollBar" TargetType="ScrollBar">
            <Setter Property="Width" Value="7"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent">
                            <Track x:Name="PART_Track"
                                   IsDirectionReversed="True"
                                   Orientation="{TemplateBinding Orientation}">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand"
                                                  Background="Transparent"
                                                  Focusable="False"
                                                  Opacity="0"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource V6DropDownThumb}"/>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand"
                                                  Background="Transparent"
                                                  Focusable="False"
                                                  Opacity="0"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="EquipmentChoiceTemplate">
            <Grid MinWidth="120">
                <TextBlock x:Name="EmptyChoice"
                           Text="{Binding Label}"
                           Foreground="#8D9AAD"
                           TextTrimming="CharacterEllipsis"
                           VerticalAlignment="Center"
                           Visibility="Collapsed"/>
                <Grid x:Name="ItemChoice">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{Binding Item.DisplayName}"
                               Foreground="{Binding RelativeSource={RelativeSource AncestorType=ComboBoxItem}, Path=Foreground}"
                               TextTrimming="CharacterEllipsis"
                               VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1"
                               Margin="10,0,0,0"
                               Text="{Binding Item.Key}"
                               Foreground="#586B82"
                               FontSize="8"
                               VerticalAlignment="Center"/>
                </Grid>
            </Grid>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding Item}" Value="{x:Null}">
                    <Setter TargetName="EmptyChoice" Property="Visibility" Value="Visible"/>
                    <Setter TargetName="ItemChoice" Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>

        <Style x:Key="V6ComboBoxItem" TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#C8D4E2"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="9,5"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <Grid>
                                <Border x:Name="ItemAccent"
                                        Width="2"
                                        Height="12"
                                        Margin="0,0,7,0"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center"
                                        Background="#6785A6"
                                        CornerRadius="1"
                                        Opacity="0"/>
                                <ContentPresenter Margin="9,0,0,0"
                                                  VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#121E2C"/>
                                <Setter TargetName="ItemAccent" Property="Opacity" Value="0.75"/>
                                <Setter Property="Foreground" Value="#F2F6FB"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#172638"/>
                                <Setter TargetName="ItemAccent" Property="Background" Value="#D2AF69"/>
                                <Setter TargetName="ItemAccent" Property="Opacity" Value="1"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#536174"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="V6ComboBox" TargetType="ComboBox">
            <Setter Property="Foreground" Value="#D8E2EE"/>
            <Setter Property="Background" Value="#0A1016"/>
            <Setter Property="BorderBrush" Value="{StaticResource Line}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,0,30,0"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource V6ComboBoxItem}"/>
            <Setter Property="MaxDropDownHeight" Value="320"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid SnapsToDevicePixels="True">
                            <Border x:Name="OuterBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6"/>

                            <Border x:Name="SelectedAccent"
                                    Width="2"
                                    Height="12"
                                    Margin="6,0,0,0"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    Background="#607B99"
                                    CornerRadius="1"
                                    Opacity="0.35"/>

                            <ContentPresenter x:Name="SelectedContent"
                                              Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Center"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              IsHitTestVisible="False"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>

                            <Border Width="30"
                                    HorizontalAlignment="Right"
                                    Background="Transparent"
                                    IsHitTestVisible="False">
                                <Path x:Name="DropArrow"
                                      Width="8"
                                      Height="5"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      Data="M 0 0 L 4 4 L 8 0"
                                      Stroke="#74879D"
                                      StrokeThickness="1.3"
                                      StrokeStartLineCap="Round"
                                      StrokeEndLineCap="Round"/>
                            </Border>

                            <ToggleButton x:Name="DropDownToggle"
                                          Focusable="False"
                                          ClickMode="Press"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent"/>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   PopupAnimation="Fade">
                                <Border x:Name="DropDownBorder"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                        Margin="0,4,0,0"
                                        Padding="4,4,2,4"
                                        Background="#0A1017"
                                        BorderBrush="#30435A"
                                        BorderThickness="1"
                                        CornerRadius="6">
                                    <ScrollViewer CanContentScroll="True"
                                                  HorizontalScrollBarVisibility="Disabled"
                                                  VerticalScrollBarVisibility="Auto">
                                        <ScrollViewer.Resources>
                                            <Style TargetType="ScrollBar" BasedOn="{StaticResource V6DropDownScrollBar}"/>
                                        </ScrollViewer.Resources>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="OuterBorder" Property="Background" Value="#0C141D"/>
                                <Setter TargetName="OuterBorder" Property="BorderBrush" Value="#3B516B"/>
                                <Setter TargetName="SelectedAccent" Property="Opacity" Value="0.7"/>
                                <Setter TargetName="DropArrow" Property="Stroke" Value="#91A6BD"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="OuterBorder" Property="BorderBrush" Value="#4C6786"/>
                                <Setter TargetName="SelectedAccent" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="OuterBorder" Property="Background" Value="#0E1722"/>
                                <Setter TargetName="OuterBorder" Property="BorderBrush" Value="#58718E"/>
                                <Setter TargetName="SelectedAccent" Property="Background" Value="#D2AF69"/>
                                <Setter TargetName="SelectedAccent" Property="Opacity" Value="1"/>
                                <Setter TargetName="DropArrow" Property="Stroke" Value="#D2AF69"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#536174"/>
                                <Setter TargetName="OuterBorder" Property="Opacity" Value="0.56"/>
                                <Setter TargetName="SelectedAccent" Property="Opacity" Value="0.15"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Tag" Value="Helm"><Setter Property="ItemTemplate" Value="{StaticResource EquipmentChoiceTemplate}"/></Trigger>
                <Trigger Property="Tag" Value="Armor"><Setter Property="ItemTemplate" Value="{StaticResource EquipmentChoiceTemplate}"/></Trigger>
                <Trigger Property="Tag" Value="Pants"><Setter Property="ItemTemplate" Value="{StaticResource EquipmentChoiceTemplate}"/></Trigger>
                <Trigger Property="Tag" Value="Gloves"><Setter Property="ItemTemplate" Value="{StaticResource EquipmentChoiceTemplate}"/></Trigger>
                <Trigger Property="Tag" Value="Boots"><Setter Property="ItemTemplate" Value="{StaticResource EquipmentChoiceTemplate}"/></Trigger>
                <Trigger Property="Tag" Value="LeftWeapon"><Setter Property="ItemTemplate" Value="{StaticResource EquipmentChoiceTemplate}"/></Trigger>
                <Trigger Property="Tag" Value="RightWeapon"><Setter Property="ItemTemplate" Value="{StaticResource EquipmentChoiceTemplate}"/></Trigger>
                <Trigger Property="Tag" Value="Wings"><Setter Property="ItemTemplate" Value="{StaticResource EquipmentChoiceTemplate}"/></Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Foreground" Value="{StaticResource Muted}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="7">
                            <ContentPresenter Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#111A25"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ToolButton" TargetType="Button">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Foreground" Value="#C5D0DD"/>
            <Setter Property="Background" Value="#0C131C"/>
            <Setter Property="BorderBrush" Value="{StaticResource Line}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="7">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#132030"/></Trigger>
            </Style.Triggers>
        </Style>
    </Application.Resources>
</Application>
